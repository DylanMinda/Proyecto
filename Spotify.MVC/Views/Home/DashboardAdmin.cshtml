@{
    ViewData["Title"] = "Dashboard Admin";
}

<h2>Bienvenido, @User.Identity.Name <small>(Administrador)</small></h2>

<div class="row gy-4">
    <!-- Gestionar Usuarios -->
    <div class="col-md-4">
        <div class="card shadow-sm">
            <div class="card-header">
                <h5 class="card-title mb-0">Gestión de Usuarios</h5>
            </div>
            <div class="card-body">
                <p>Ver, editar roles o eliminar cuentas de usuarios.</p>
                <a asp-controller="Usuarios" asp-action="Usuarios" class="btn btn-outline-primary">Ir a Usuarios</a>
            </div>
        </div>
    </div>

    <!-- Gestión de Canciones -->
    <div class="col-md-4">
        <div class="card shadow-sm">
            <div class="card-header">
                <h5 class="card-title mb-0">Gestión de Canciones</h5>
            </div>
            <div class="card-body">
                <p>Ver, editar o eliminar canciones.</p>
                <a asp-controller="Canciones" asp-action="Index" class="btn btn-outline-primary">Ir a Canciones</a>
            </div>
        </div>
    </div>

    <!-- Otros módulos de administración -->
    <div class="col-md-4">
        <div class="card shadow-sm">
            <div class="card-header">
                <h5 class="card-title mb-0">Otro módulo</h5>
            </div>
            <div class="card-body">
                <p>Este es otro módulo de administración.</p>
                <a asp-controller="Admin" asp-action="OtroModulo" class="btn btn-outline-primary">Ir a módulo</a>
            </div>
        </div>
    </div>
</div>
